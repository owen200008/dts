<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.utopia.data.transfer.admin.dao.mapper.base.PipelineBeanRepository">
    <resultMap id="BaseResultMap" type="com.utopia.data.transfer.admin.dao.entity.PipelineBean">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="task_id" jdbcType="INTEGER" property="taskId"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="source_entity_id" jdbcType="INTEGER" property="sourceEntityId"/>
        <result column="target_entity_id" jdbcType="INTEGER" property="targetEntityId"/>
        <result column="modify_time" jdbcType="TIMESTAMP" property="modifyTime"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>
    <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="com.utopia.data.transfer.admin.dao.entity.PipelineBean">
        <result column="pipeline_params" jdbcType="LONGVARCHAR" property="pipelineParams"/>
    </resultMap>

    <resultMap id="excludeResultMap" type="com.utopia.data.transfer.admin.dao.entity.PipeDetail">
        <id column="id"  jdbcType="INTEGER" property="id"></id>
        <result column="name" jdbcType="VARCHAR" property="name"></result>
        <result column="sourceEntityName" jdbcType="VARCHAR" property="sourceEntityName"></result>
        <result column="sourceEntityId" jdbcType="INTEGER" property="sourceEntityId"></result>
        <result column="targetEntityName" jdbcType="VARCHAR" property="targetEntityName"></result>
        <result column="targetEntityId" jdbcType="INTEGER" property="targetEntityId"></result>
    </resultMap>
    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Update_By_Example_Where_Clause">
        <where>
            <foreach collection="example.oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Base_Column_List">

            id, task_id, `name`, source_entity_id, target_entity_id, modify_time, create_time

    </sql>
    <sql id="Blob_Column_List">

            pipeline_params

    </sql>

    <select id="selectDetailById" parameterType="java.lang.Integer" resultMap="excludeResultMap">
           SELECT
            *
        FROM
            (
            SELECT
                pi.id,
                pi.NAME,
                en.NAME AS sourceEntityName,
                en.id AS sourceEntityId
            FROM
                u_pipeline pi
                INNER JOIN u_entity en ON pi.source_entity_id = en.id
            WHERE
                pi.id = #{id}
            ) AS source,
            (
            SELECT
                en.NAME AS targetEntityName,
                en.id AS targetEntityId
            FROM
                u_pipeline pi
                INNER JOIN u_entity en ON pi.target_entity_id = en.id
            WHERE
                pi.id = #{id}
            ) AS target
    </select>
</mapper>